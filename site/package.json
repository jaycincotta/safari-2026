{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "npm run media:optimize && astro dev",
    "build": "npm run media:optimize && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "media:optimize": "node ./scripts/process-media.mjs"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}